To use this tool on the web, https://quadminds.github.io/firebase-getJWT/
You will need to add the domain quadminds.github.io
in Firebase > Auth Section > Auth Domain

Add config params to url as query params, like:
?apiKey={API_KEY}&authDomain={AUTH_DOMAIN}&databaseURL={DB_URL}&storageBucket={STORAGE_URL}
